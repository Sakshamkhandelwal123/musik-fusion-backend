type Chat {
  # Example field (placeholder)
  exampleField: Int
}

input CreateChatInput {
  message: String!
  channelId: String!
}

input UpdateChatInput {
  id: Int!
}

type Query {
  getAllFriendsChannel: Friend!
}

type Mutation {
  joinChannel(friendUserId: String!): Chat!
  sendMessage(createChatInput: CreateChatInput!): Chat!
  removeChat(id: Int!): Chat
}
